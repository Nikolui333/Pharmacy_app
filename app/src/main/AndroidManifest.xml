<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.semenov.pharmacy_app">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Pharmacy_app">
        <activity
            android:name=".preparationActivity.CureSearchActivity"
            android:exported="true" />
        <activity android:name=".clientsActivity.ClientAddActivity" />
        <activity android:name=".clientsActivity.ContactsClientActivity" />
        <activity android:name=".clientsActivity.ClientsListActivity" />
        <activity android:name=".preparationActivity.DescriptionPreparationActivity" />
        <activity android:name=".preparationActivity.PreparationAddActivity" />
        <activity android:name=".preparationActivity.PreparationListActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>